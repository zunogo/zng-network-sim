services:
  - type: web
    name: zng-simulator-api
    runtime: python
    plan: free
    buildCommand: pip install -e .
    startCommand: uvicorn zng_simulator.api.server:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /health
    envVars:
      - key: PYTHON_VERSION
        value: "3.12"
